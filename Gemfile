source "https://rubygems.org"

# Pin to GitHub Pages' supported Jekyll + plugin set so local builds match what
# GitHub Pages will run after a push.
gem "github-pages", group: :jekyll_plugins

# Needed on Ruby 3.x for `jekyll serve` (Webrick was removed from stdlib).
gem "webrick", "~> 1.8"
